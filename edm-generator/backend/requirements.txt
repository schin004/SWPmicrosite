# Core web framework
fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0

# Data / persistence
SQLAlchemy>=2.0,<3.0

# Config & validation
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
python-dotenv>=1.0,<2.0

# HTTP + scraping
httpx>=0.27,<1.0
beautifulsoup4>=4.12,<5.0
lxml>=5.2

# Optional scheduled auto-refresh
APScheduler>=3.10,<4.0

# Optional AI summaries (a deterministic fallback is used when unavailable)
anthropic>=0.34
